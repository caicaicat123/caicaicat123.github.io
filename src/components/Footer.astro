---
import { extraNav, nav, site } from '../data/site';

const year = new Date().getFullYear();
const yearRange = String(year) === site.startedAt ? site.startedAt : `${site.startedAt}–${year}`;
const related = [...nav.slice(4), ...extraNav];
---

<footer class="relative mt-24 border-t border-line">
  <div class="mx-auto max-w-6xl px-4 py-14">
    <div class="grid gap-10 sm:grid-cols-2 lg:grid-cols-4">
      <div class="sm:col-span-2">
        <div class="flex items-center gap-3">
          <img
            src="/icon.png"
            alt=""
            width="295"
            height="349"
            class="h-9 w-auto shrink-0"
            aria-hidden="true"
          />
          <span class="flex flex-col leading-none">
            <strong class="text-[14.5px] font-bold tracking-[0.14em] text-strong">{site.name}</strong>
            <span class="font-pixel mt-1 text-[9.5px] text-muted">{site.nameEn}</span>
          </span>
        </div>
        <p class="mt-5 max-w-md text-sm leading-[1.85] text-muted">{site.vision}</p>
      </div>

      <div>
        <p class="font-pixel mb-4 text-[11px] text-muted">导航</p>
        <ul class="space-y-2.5 text-sm">
          {
            nav.slice(0, 4).map((item) => (
              <li>
                <a href={item.href} class="text-muted transition-colors hover:text-ink">
                  {item.label}
                </a>
              </li>
            ))
          }
        </ul>
      </div>

      <div>
        <p class="font-pixel mb-4 text-[11px] text-muted">相关</p>
        <ul class="space-y-2.5 text-sm">
          {
            related.map((item) => (
              <li>
                <a href={item.href} class="text-muted transition-colors hover:text-ink">
                  {item.label}
                </a>
              </li>
            ))
          }
          <li>
            <a href="/rss.xml" class="text-muted transition-colors hover:text-ink">RSS 订阅</a>
          </li>
          <li>
            <a
              href={site.github}
              rel="noopener noreferrer"
              target="_blank"
              class="text-muted transition-colors hover:text-ink">GitHub</a
            >
          </li>
        </ul>
      </div>
    </div>
  </div>

  <div class="border-t border-line">
    <div
      class="mx-auto flex max-w-6xl flex-col gap-2 px-4 py-6 text-xs text-muted sm:flex-row sm:items-center sm:justify-between"
    >
      <p>Copyright © {yearRange} {site.nameEn}, All Rights Reserved.</p>
      <p class="font-pixel text-[10px]">{site.slogan}</p>
    </div>
  </div>
</footer>
